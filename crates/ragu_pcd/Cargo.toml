[package]
name = "ragu_pcd"
version.workspace = true
authors.workspace = true
description = """
Intended for internal use in the Ragu project
"""
license.workspace = true
edition.workspace = true
rust-version.workspace = true

readme = "README.md"
repository.workspace = true
documentation = "https://docs.rs/ragu_pcd"
categories = [
  "cryptography",
  "mathematics",
  "algorithms",
  "development-tools",
  "no-std",
]
keywords = []

[package.metadata.docs.rs]
rustdoc-args = ["--html-in-header", "katex-header.html"]
all-features = true

[features]
accel-msm = ["std", "ragu_arithmetic/accel-msm", "ragu_circuits/accel-msm"]
alloc = []
default = ["alloc"]
multicore = ["maybe-rayon/threads", "ragu_arithmetic/multicore", "ragu_circuits/multicore", "std"]
std = []
# Exposes the `fuzz_utils` module, which provides proof-corruption helpers
# used by external fuzz harnesses. Not part of the stable public API.
unstable-fuzzing = []

[lib]
bench = false

[dependencies]
ragu_arithmetic = { path = "../ragu_arithmetic", version = "0.0.0" }
ff = { workspace = true }
maybe-rayon = { workspace = true }
pasta_curves = { workspace = true }
ragu_circuits = { path = "../ragu_circuits", version = "0.0.0" }
ragu_core = { path = "../ragu_core", version = "0.0.0" }
ragu_primitives = { path = "../ragu_primitives", version = "0.0.0" }
rand = { workspace = true }

[dev-dependencies]
criterion = { workspace = true }
ragu_pasta = { path = "../ragu_pasta", version = "0.0.0", features = ["baked"] }
gungraun = { workspace = true }
ragu_testing = { path = "../ragu_testing", version = "0.0.0" }
proptest = { workspace = true }

[[bench]]
name = "pcd"
harness = false

[[bench]]
name = "fuse_criterion"
path = "benches/criterion/fuse.rs"
harness = false

[[bench]]
name = "registry_criterion"
path = "benches/criterion/registry.rs"
harness = false

[[bench]]
name = "pcd_accel_criterion"
path = "benches/criterion/pcd_accel.rs"
harness = false
